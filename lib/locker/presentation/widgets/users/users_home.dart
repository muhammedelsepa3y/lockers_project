import 'package:flutter/material.dart';

class UsersHome extends StatelessWidget {
  const UsersHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Users Home"),
    );
  }
}
