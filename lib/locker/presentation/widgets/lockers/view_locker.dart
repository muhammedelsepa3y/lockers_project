import 'package:flutter/material.dart';

class ViewLocker extends StatelessWidget {
  const ViewLocker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("View Locker"),
    );
  }
}
