import 'package:flutter/material.dart';

class ManageLocker extends StatelessWidget {
  const ManageLocker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Manage Locker"),
    );
  }
}
